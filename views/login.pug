 
head 
  link#bootstrap-css(href='//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css' rel='stylesheet')
  script(src='//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js')
  script(src='//code.jquery.com/jquery-1.11.1.min.js')
  // ---- Include the above in your HEAD tag --------
.sidenav
  .login-main-text
    h2
      | BUZZFED
      br
      |  Login Page
    p Login or register from here to access.
.main
  .col-md-6.col-sm-12
    .login-form
      form#login-form.form(action = '/login' method = 'post')
        .form-group
          label Username
          input#username.form-control(type='text' placeholder ='Username')
        .form-group
          label Password
          input#password.form-control(type='password' placeholder = 'Password')
        button.btn.btn-black(type='submit') Logga in
        button.btn.btn-secondary(type='submit') Register
